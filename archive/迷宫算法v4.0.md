# 迷宫生成方案 v4.0 - 含房间的区域式生成

## 一、v4 相比 v3 的核心变化

v3 的区域是均匀的10×10格方块，区域之间没有明确隔离但有类型差异。v4 在此基础上引入**房间**——散布在迷宫中的空旷大空间，尺寸4×4到8×8格，打断连续的狭窄通道，成为探索的节奏锚点和未来玩法的载体。

**v3 保留的部分**：
- 区域划分的思想（大地图先分区块再细化）
- 区域级通路生成（主干道、分支、替代路径、封闭区域）
- 区域类型分配（start/end/main/branch/junction/closed）

**v4 新增的部分**：
- 房间作为独立的空间单元，有明确的边界和入口
- 房间生成优先于通道生成（先放房间，再用通道连接）
- 房间类型系统（不同房间有不同功能和视觉特征）

**v4 调整的部分**：
- 区域不再是10×10的均匀网格，而是灵活的空间容器
- 通道生成不再是填充区域，而是连接房间和区域边界

---

## 二、设计理念：从"通道迷宫"到"房间-通道迷宫"

传统迷宫是"墙壁和通道"的二元对立。v4 的迷宫是三层空间结构：

```
房间（大空间，4×4到8×8格）
  ↕ 通过门口连接
通道（1-2格宽的走廊）
  ↕ 通道在区域边界处交汇
区域边界（概念上的区块分界，玩家不可见）
```

玩家体验变成：
> 我在狭窄的通道里走了一段 → 突然进入一个开阔的房间 → 房间里有宝箱/特殊物品/多条出口 → 我选择一个出口继续探索 → 又进入通道 → 期待下一个房间

这种"收→放→收→放"的节奏，是《以撒的结合》《死亡细胞》等游戏探索乐趣的重要来源。

---

## 三、全局参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 大地图尺寸 | 100×100格 | 总网格 |
| 区域概念划分 | 5×5个区块 | 每个区块约20×20格（概念性，非硬边界） |
| 房间总数 | 25-40个 | 散布全图 |
| 房间最小尺寸 | 4×4格 | 含墙壁 |
| 房间最大尺寸 | 8×8格 | 含墙壁 |
| 通道宽度 | 1-2格 | 连接房间的走廊宽度 |
| 起点终点最小距离 | 曼哈顿距离≥3个区块 | 保证穿越多个房间 |

---

## 四、生成流程概览

```
阶段一：划分概念区块，确定起点终点区块
阶段二：生成房间位置和尺寸
阶段三：分配房间类型
阶段四：生成房间之间的通道连接
阶段五：填充剩余空间（死胡同、回路、封闭区域）
阶段六：放置宝箱和特殊物品
阶段七：验证
```

---

## 五、阶段一：划分概念区块并确定起点终点

### 5.1 目的

将100×100格的大地图概念性地划分为5×5个**区块**（每个区块约20×20格），在区块层面确定起点和终点的位置。区块不是硬边界，只用于控制房间分布的宏观节奏。

### 5.2 算法步骤

**步骤1：划分区块**
```
将100×100格划分为5×5个概念区块
每个区块覆盖约20×20格
区块之间没有硬边界，房间可以跨越区块边界（但中心必须在某个区块内）
```

**步骤2：确定起点区块和终点区块**
```
起点区块：随机选，建议在第0行或第4行（大地图上下边缘）
终点区块：随机选，约束：
  - 与起点区块的曼哈顿距离 ≥ 3
  - 不在大地图最外圈（即不在行0和行4同时、也不在列0和列4同时）
  - 保证从起点到终点至少穿越3-4个区块
```

**步骤3：规划区块级路径**
```
在5×5的区块层面，规划从起点区块到终点区块的路径：
  - 1条主路径（经过3-5个区块）
  - 1-2条替代路径（和主路径有部分重叠但不完全相同）
  - 若干分支区块（连接在主路径上，向外延伸1-2个区块）
路径上的区块称为"活跃区块"，后续会放置较多的房间
不在路径上的区块称为"边缘区块"，房间密度较低，可能有封闭区域
```

---

## 六、阶段二：生成房间

### 6.1 房间数据结构

```
Room {
    id: string,
    centerX: number,        // 房间中心X坐标（格单位，0-99）
    centerY: number,        // 房间中心Y坐标（格单位，0-99）
    width: number,          // 房间宽度（4-8格，含墙壁）
    height: number,         // 房间高度（4-8格，含墙壁）
    type: string,           // 阶段三分配
    connections: [],        // 该房间连接的其他房间ID列表
    entrancePositions: [],  // 门口在房间边界上的具体格子位置
    blockIndex: {row, col}  // 所在区块
}
```

### 6.2 房间放置算法

房间必须满足两个核心约束：
- **不重叠**：任意两个房间的矩形区域不能重叠，且保持至少2格间距（留给通道）
- **可达性**：所有房间必须通过通道连接成一个连通图

**步骤1：在活跃区块中放置房间**

```
活跃区块 = 主路径区块 + 替代路径区块 + 分支区块
边缘区块 = 其余区块

对于每个活跃区块：
    放置房间数量 = 随机 2-4 个
    对于每个要放置的房间：
        1. 随机房间尺寸：width = 随机 4-8，height = 随机 4-8
        2. 随机房间位置：
           房间中心X = 区块左上角X + 随机偏移（保证房间不超出区块边界）
           房间中心Y = 区块左上角Y + 随机偏移
        3. 检查与已有房间是否重叠（含2格间距）：
           新房间矩形 = [centerX - width/2 - 2, centerY - height/2 - 2,
                         centerX + width/2 + 2, centerY + height/2 + 2]
           遍历已有房间，检查矩形是否相交
           如果重叠，重新随机位置（最多尝试10次）
        4. 如果10次都重叠，减小房间尺寸再试（最小4×4）
        5. 如果仍重叠，跳过该房间
```

**步骤2：在边缘区块中放置房间**

```
对于每个边缘区块：
    放置房间数量 = 随机 0-2 个
    放置规则同上
    边缘区块的房间有30%概率被标记为"封闭候选"（后续可能成为封闭区域）
```

**步骤3：起点房间和终点房间**

```
起点房间：
  - 放在起点区块内
  - 尺寸固定为 5×5 或 6×6
  - 标记为起点房间（玩家出生点在其中）
  - 强制放置，如果该区块没有空间则扩大尝试范围到相邻区块

终点房间：
  - 放在终点区块内
  - 尺寸固定为 6×6 或 7×7
  - 标记为终点房间（核心遗物在其中）
  - 房间内有一个特殊的中心区域（放遗物）
  - 强制放置，优先级最高
```

**步骤4：验证房间分布**

```
房间总数应在 25-40 个之间
如果不够：
  - 在房间密度低的区块补充放置
  - 优先在活跃区块补充
如果超出：
  - 移除边缘区块中尺寸最小的房间
```

### 6.3 房间生成后的地图写入

每个房间在地图上的实际占地区域：

```
房间内部（不含墙壁）：
  X范围：[centerX - width/2 + 1, centerX + width/2 - 1]
  Y范围：[centerY - height/2 + 1, centerY + height/2 - 1]
  这些格子标记为 "room_floor"

房间墙壁：
  房间内部区域的边界外一圈
  这些格子标记为 "room_wall"

房间墙壁上的门口：
  在房间墙壁上留出的1-2格宽开口
  开口位置：阶段四确定（根据房间之间的连接关系）
  这些格子标记为 "room_door"（暂时，连接后改为 "floor"）
```

---

## 七、阶段三：分配房间类型

### 7.1 房间类型定义

| 类型 | 尺寸倾向 | 内部特征 | 出现位置 |
|------|----------|----------|----------|
| start | 5×5 或 6×6 | 安全、明亮、2-3个出口 | 起点区块 |
| end | 6×6 或 7×7 | 中心有遗物台座、2-3个入口 | 终点区块 |
| hall | 6×6 到 8×8 | 开阔大厅、多出口（3-4个）、可能有柱子 | 主路径上 |
| chamber | 4×4 到 6×6 | 小型房间、1-2个出口、通常有宝箱 | 分支路径上 |
| vault | 5×5 到 7×7 | 封闭房间、1个入口、高价值宝箱 | 边缘区块（封闭候选） |
| crossing | 5×5 到 7×7 | 十字路口型、4个出口、中央有地标 | 路径交汇处 |
| deadend | 4×4 到 5×5 | 死胡同房间、1个入口、宝箱在深处 | 分支末端 |
| hidden | 4×4 到 6×6 | 隐藏房间、需要特殊方式进入 | 随机散布 |

### 7.2 分配算法

```
对于每个房间：

    if 房间是起点房间：
        type = "start"
    
    else if 房间是终点房间：
        type = "end"
    
    else if 房间所在区块在主路径和替代路径的交汇处：
        type = "crossing"  （概率70%）或 "hall"（概率30%）
    
    else if 房间所在区块在主路径上（非交汇）：
        type = "hall"（概率60%）或 "chamber"（概率40%）
    
    else if 房间所在区块在分支路径上：
        if 房间是分支末端：
            type = "deadend"（概率70%）或 "chamber"（概率30%）
        else：
            type = "chamber"（概率80%）或 "hall"（概率20%）
    
    else if 房间被标记为"封闭候选"：
        type = "vault"（概率60%）或 "hidden"（概率40%）
    
    else：
        type = "chamber"（默认）
```

### 7.3 房间内部结构生成

根据房间类型，在房间内部生成不同的结构：

**start 房间**：
```
内部完全空旷（无内部墙壁）
地板使用特殊纹理
玩家出生点在房间中央
有2-3个出口（门开在房间墙壁上）
```

**end 房间**：
```
内部完全空旷
中央有一个 2×2 的"遗物台座"（不可通行的装饰性地块）
遗物放在台座中央
有2-3个入口
```

**hall 房间**：
```
内部有 0-2 根柱子（1×1不可通行格）
柱子随机放置，但不能堵住任何门口之间的通路
有3-4个出口
```

**chamber 房间**：
```
内部完全空旷或只有1根柱子
有1-2个出口
如果只有1个出口，宝箱放在远离出口的深处
```

**vault 房间**：
```
内部可能有1-2面半墙（1格宽的短墙，制造视线遮挡）
只有1个入口
宝箱放在最深处
```

**crossing 房间**：
```
内部中央有1个地标（柱子/雕像/喷泉，1×1或2×2不可通行格）
4个出口在四个方向
地标不堵路，出口之间保持通畅
```

**deadend 房间**：
```
内部完全空旷
只有1个入口
宝箱放在房间最深处（离入口最远的位置）
```

**hidden 房间**：
```
内部完全空旷或只有1根柱子
入口被特殊墙壁封闭（需要胶囊才能进入）
内部有高价值宝箱
```

---

## 八、阶段四：生成房间之间的通道连接

### 8.1 目的

在所有房间之间生成通道，使它们连接成一个连通图，且保证从起点房间到终点房间有至少2条路径。

### 8.2 确定房间连接关系

使用**德劳内三角化 + 最小生成树 + 额外边**的方式确定哪些房间之间需要通道。

**步骤1：构建房间连接候选图**

```
以每个房间的中心点为节点
计算所有房间对之间的欧几里得距离
对每个房间，找到距离最近的3-5个邻居（K近邻）
在房间和其近邻之间建立候选连接边
边的权重 = 两个房间中心点之间的直线距离
```

**步骤2：生成最小生成树（保证连通）**

```
在候选连接图上运行 Prim 算法
生成最小生成树（所有房间连通，且总距离最小）
最小生成树中的边 = 必须存在的通道
```

**步骤3：添加额外连接（创造回路）**

```
对于最小生成树之外的候选边：
    以概率 P_extra 添加到连接中
    
    P_extra 取决于两个房间所在的区块类型：
    - 活跃区块（主路径/替代路径）：P_extra = 40%
    - 分支区块：P_extra = 25%
    - 边缘区块：P_extra = 10%
    
    额外连接的数量上限 = 房间总数 × 0.3
```

**步骤4：处理封闭房间**

```
vault 和 hidden 类型的房间：
    只保留1条连接（从最小生成树中选择最短的那条）
    移除其他所有连接
    这条唯一的连接在生成通道时，会有一个明显的"入口标记"
```

**步骤5：验证多路径**

```
从起点房间出发，DFS/BFS遍历房间连接图
确认终点房间可达
确认从起点到终点至少有2条不共享关键节点的路径
如果不满足，在起点和终点之间的两个不同分支上手动添加一条连接
```

### 8.3 生成房间之间的通道

确定了哪些房间之间有连接后，在地图上生成连接它们的通道。

**通道生成算法**：

```
对于每对有连接的房间（RoomA, RoomB）：
    
    1. 确定门口位置：
       在 RoomA 的墙壁上，选择最靠近 RoomB 方向的一面墙
       在该面墙上开一个1-2格宽的门口
       同样在 RoomB 的墙壁上开门口
       门口位置：选择墙面中间位置，避开墙角
    
    2. 生成连接通道：
       从 RoomA 的门口出发，向 RoomB 的门口方向延伸
       使用 A* 寻路算法：
          - 起点：RoomA 门口格
          - 终点：RoomB 门口格
          - 地图：当前100×100格（已有房间占用的格标记为不可通行）
          - 启发式：曼哈顿距离
       
       A* 找到的路径上的格子标记为 "corridor"
    
    3. 通道宽度：
       主路径上的通道：宽度 2 格（更宽敞）
       其他通道：宽度 1 格
    
    4. 通道风格：
       通道必须是直来直去的（允许直角转弯）
       通道不可以穿过任何房间的内部区域
       通道如果经过另一个房间的墙壁，必须绕过
```

**通道与房间的接口处理**：

```
门口格在房间墙壁上：
  该格子的类型从 "room_wall" 改为 "room_door"
  然后在连接完成后统一改为 "floor"
  这样门口格就是房间和通道的自然过渡
```

---

## 九、阶段五：填充剩余空间

房间和主要通道生成完毕后，地图上还有一些未被使用的空间。用额外的通道和死胡同填充它们，让迷宫更丰富。

### 9.1 分支通道

```
从已有通道或房间的门口出发：
    随机选择一些点作为分支起点
    向未填充区域延伸，生成 2-6 格长的分支通道
    分支通道末端可以：
      - 形成死胡同（60%概率）
      - 连接到另一个已有通道（30%概率，创造额外回路）
      - 放置一个宝箱或特殊物品（10%概率）
```

### 9.2 小型死胡同

```
在主通道旁随机生成1-3格的小型死胡同
这些死胡同里可以：
  - 放置火把残骸（补给）
  - 放置小型宝箱
  - 什么都不放（只是增加迷宫复杂度）
```

### 9.3 封闭区域处理

```
对于 vault 和 hidden 类型的房间：
    它们只有1个入口
    入口处的通道可以比其他通道更窄（1格宽）
    入口处放置视觉标记（特殊墙壁纹理）
    对于 hidden 类型，入口用特殊墙壁封闭
```

---

## 十、阶段六：放置宝箱和特殊物品

### 10.1 宝箱放置规则

| 房间类型 | 宝箱数量 | 宝箱位置 | 特殊说明 |
|----------|----------|----------|----------|
| start | 0 | - | 起点房间无宝箱 |
| end | 1 | 遗物台座旁 | 通关奖励预览 |
| hall | 1-2 | 柱子旁或墙角 | 普通或条件宝箱 |
| chamber | 1-2 | 远离出口的深处 | 如果是单出口，宝箱在深处 |
| vault | 2-3 | 最深处 | 宝箱品质提升一级，60%条件宝箱 |
| crossing | 0-1 | 中央地标旁 | 通常是豪华宝箱 |
| deadend | 1-2 | 房间最深处 | 50%条件宝箱 |
| hidden | 1-2 | 房间中央或深处 | 宝箱品质最高，必定有稀有胶囊 |

### 10.2 通道中的补给和宝箱

```
在通道中散布：
  - 火把残骸：每3-5条通道有1个，放在通道旁
  - 小型宝箱：每5-8条通道有1个，放在死胡同末端
  - 独石碑（地标）：每2-3个房间之间的通道交汇处有1个
```

### 10.3 特殊物品放置

```
残垣断壁（可通过的假墙）：在封闭区域入口附近放1-2个
高消耗格（经过时消耗额外火把）：在捷径处放1-2个
特殊标记（暗示隐藏房间）：在 hidden 房间入口附近放1个微弱标记
```

---

## 十一、阶段七：验证

### 11.1 连通性验证

```
从起点房间的玩家出生格出发，BFS遍历所有 floor 格
确认：
  - 终点房间的遗物格在可达集合中
  - 所有宝箱所在格在可达集合中
  - 所有房间至少有一个入口/出口是可达的
  - hidden 房间的特殊墙壁确实需要胶囊才能打开（但仍计入可达性验证中）
```

### 11.2 路径长度验证

```
计算从起点到终点的最短路径长度（曼哈顿距离，沿 floor 格走）
最短路径应在 80-200 格之间
如果太短（<80格）：在路径上增加额外弯折或添加必须绕过的障碍
如果太长（>200格）：检查是否因为回路过多导致，适当减少额外连接
```

### 11.3 房间密度验证

```
房间总数应在 25-40 个之间
房间之间的平均距离（通道长度）应在 5-15 格之间
如果通道太长（>20格），在通道中间补充一个 chamber 或 deadend 房间
```

### 11.4 宝箱分布验证

```
检查是否连续3个房间以上没有宝箱，如果有，在中间房间补充1个
检查 hidden 和 vault 房间是否有宝箱，如果没有，补充
检查起点房间是否有宝箱，如果有，移除
```

---

## 十二、完整生成流程伪代码

```
function GenerateFullMazeV4():

    // 阶段一：概念区块 + 起点终点
    blocks = DivideIntoBlocks(5, 5)  // 5×5概念区块
    startBlock = SelectStartBlock(blocks)
    endBlock = SelectEndBlock(blocks, startBlock, minDistance=3)
    activeBlocks = PlanBlockPaths(blocks, startBlock, endBlock)
    
    // 阶段二：生成房间
    rooms = []
    for each block in activeBlocks:
        rooms.append(PlaceRoomsInBlock(block, count=2-4))
    for each block in edgeBlocks:
        rooms.append(PlaceRoomsInBlock(block, count=0-2))
    startRoom = ForcePlaceStartRoom(startBlock, size=5-6)
    endRoom = ForcePlaceEndRoom(endBlock, size=6-7)
    rooms.append(startRoom, endRoom)
    ValidateRoomCount(rooms, 25-40)
    
    // 阶段三：分配房间类型
    AssignRoomTypes(rooms, activeBlocks)
    GenerateRoomInteriors(rooms)
    
    // 阶段四：生成房间连接和通道
    candidateEdges = BuildKNNEdges(rooms, k=5)
    mst = PrimMST(candidateEdges)  // 最小生成树保证连通
    extraEdges = AddExtraEdges(candidateEdges, mst, probability)
    allEdges = mst + extraEdges
    ProcessVaultAndHiddenRooms(allEdges, rooms)  // 封闭房间只留1条连接
    ValidateMultiPath(allEdges, startRoom, endRoom)  // 确认多路径
    GenerateCorridors(allEdges, rooms)  // A*生成通道
    
    // 阶段五：填充剩余空间
    GenerateBranchCorridors()
    GenerateSmallDeadEnds()
    MarkVaultEntrances()
    MarkHiddenWalls()
    
    // 阶段六：放置宝箱和特殊物品
    PlaceTreasureChests(rooms)
    PlaceCorridorPickups()
    PlaceSpecialTerrain()
    
    // 阶段七：验证
    ValidateConnectivity(startRoom, endRoom, rooms)
    ValidatePathLength(startRoom, endRoom, min=80, max=200)
    ValidateRoomDensity(rooms)
    ValidateChestDistribution(rooms)
    
    return map
```

---

## 十三、参数速查表

| 参数 | 建议值 | 说明 |
|------|--------|------|
| 大地图尺寸 | 100×100格 | 总网格 |
| 概念区块 | 5×5个 | 每个约20×20格 |
| 房间总数 | 25-40个 | 全地图 |
| 房间最小尺寸 | 4×4格 | 含墙壁 |
| 房间最大尺寸 | 8×8格 | 含墙壁 |
| 房间最小间距 | 2格 | 留给通道 |
| 活跃区块每块房间数 | 2-4个 | 主路径/替代路径/分支 |
| 边缘区块每块房间数 | 0-2个 | 其余区块 |
| 起点房间尺寸 | 5×5 或 6×6 | - |
| 终点房间尺寸 | 6×6 或 7×7 | - |
| 通道宽度（主路径） | 2格 | 连接主路径房间 |
| 通道宽度（其他） | 1格 | 连接其他房间 |
| K近邻数量 | 5 | 构建候选连接 |
| 额外连接概率（活跃区块） | 40% | - |
| 额外连接概率（分支区块） | 25% | - |
| 额外连接概率（边缘区块） | 10% | - |
| 分支通道长度 | 2-6格 | 填充未使用空间 |
| 最短路径范围 | 80-200格 | 起点到终点 |

---

以上是 v4 完整方案。核心改动是引入了房间作为独立的空间单元，通道变成了连接房间的走廊，而不是迷宫的本体。这为你后续在房间里加入有趣玩法（机关、谜题、遭遇等）提供了载体。