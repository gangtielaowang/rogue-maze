# 迷宫生成方案 v5.0 - 动态区块 + 灵活房间系统

## 一、v5 相比 v4 的核心变化

| 维度 | v4 | v5 |
|------|-----|-----|
| 区块划分 | 固定5×5，每块20×20 | 动态x，x∈[12,20]，区块数自动计算 |
| 房间尺寸 | 固定4×4到8×8 | 灵活4×4到16×16，分档控制比例 |
| 大房间实现 | 无 | 支持2个相邻区块合并形成超大房间 |
| 房间-区块关系 | 一对一（房间在区块内） | 一对多（一个区块可有多个小房间）或多对一（两个区块合并一个房间） |
| 边缘处理 | 无 | 取整后边缘留白做边界墙 |

---

## 二、全局参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 大地图尺寸 | 100×100格 | 总网格 |
| 区块边长 x | 随机12-20 | 开局随机，决定区块数量 |
| 区块数量 | floor(100/x) × floor(100/x) | 如x=12→8×8=64个区块 |
| 边缘留白 | 100 - (区块数 × x) 格 | 分布在大地图右侧和底部边缘 |
| 房间最小尺寸 | 4×4格 | 含墙壁 |
| 房间最大尺寸 | 16×16格 | 含墙壁（需合并区块支持） |
| 房间总数 | 20-50个 | 动态，取决于x和区块数 |
| 房间最小间距 | 2格 | 留给通道 |
| 合并区块上限 | 2个 | 仅限相邻区块 |
| 起点终点最小距离 | 曼哈顿距离≥3个区块 | - |

---

## 三、数据结构

### 3.1 区块

```
Block {
    id: string,              // "block_r_c"
    row: number,             // 在区块网格中的行
    col: number,             // 在区块网格中的列
    gridRow: number,         // 在大地图中的起始行（格坐标）
    gridCol: number,         // 在大地图中的起始列（格坐标）
    size: number,            // 边长 = x
    isMerged: boolean,       // 是否与相邻区块合并
    mergedWith: string|null, // 合并目标区块的ID
    rooms: [],               // 该区块包含的房间ID列表
    isActive: boolean        // 是否在路径上（活跃区块）
}
```

### 3.2 房间

```
Room {
    id: string,
    row: number,             // 房间在大地图中的起始行（格坐标）
    col: number,             // 房间在大地图中的起始列（格坐标）
    width: number,           // 房间宽度（4-16，含墙壁）
    height: number,          // 房间高度（4-16，含墙壁）
    type: string,            // start/end/hall/chamber/vault/crossing/deadend/hidden
    blocks: [],              // 所属区块ID列表（1个或2个）
    connections: [],         // 连接到的房间ID列表
    doors: [],               // 门口在房间边界上的具体位置 [{row, col, direction}]
    interiorWalls: [],       // 内部柱子/半墙的位置
}
```

### 3.3 大地图

```
Map {
    cells: Cell[100][100],   // 每个格子的类型
    blocks: Block[][],       // 区块二维数组
    rooms: Room[],           // 所有房间列表
    startRoomId: string,
    endRoomId: string
}

Cell {
    type: "wall" | "floor" | "room_floor" | "room_wall" | "door" | 
          "special_wall" | "treasure" | "torch_pickup" | "landmark" | 
          "start" | "exit"
}
```

---

## 四、生成流程概览

```
阶段一：确定x，划分区块，处理边缘留白
阶段二：确定起点终点区块，规划区块级路径
阶段三：确定区块合并方案
阶段四：放置房间（大房间优先，小房间填充）
阶段五：分配房间类型
阶段六：生成房间内部结构
阶段七：生成房间之间的通道连接
阶段八：填充剩余空间（额外通道、死胡同）
阶段九：放置宝箱和特殊物品
阶段十：验证
```

---

## 五、阶段一：确定x，划分区块，处理边缘留白

### 5.1 确定x

```
x = 随机整数，范围 [12, 20]
未来可扩展为根据参数计算（如玩家人数、强度等）
```

### 5.2 计算区块数量

```
blockCount = floor(100 / x)
// 例如 x=12 → blockCount=8（8×8=64个区块，覆盖96×96格）
// 例如 x=20 → blockCount=5（5×5=25个区块，覆盖100×100格）
```

### 5.3 划分区块网格

```
创建 blockCount × blockCount 的 Block 二维数组

对于每个区块 (r, c)，其中 r ∈ [0, blockCount-1], c ∈ [0, blockCount-1]：
    block.gridRow = r × x
    block.gridCol = c × x
    block.size = x
    block.isMerged = false
    block.mergedWith = null
    block.rooms = []
    block.isActive = false  // 阶段二确定
```

### 5.4 处理边缘留白

```
usedSize = blockCount × x
remaining = 100 - usedSize

// 右侧留白（如果 remaining > 0）
右侧留白列范围：[usedSize, 99]
这些列的所有行标记为 "wall"（不可进入的边界墙）

// 底部留白（如果 remaining > 0）
底部留白行范围：[usedSize, 99]
这些行的所有列标记为 "wall"
```

### 5.5 输出

- x 的值
- blockCount 的值
- 完整的 Block 二维数组
- 留白区域已被标记为边界墙的地图

---

## 六、阶段二：确定起点终点区块，规划区块级路径

### 6.1 确定起点终点区块

```
起点区块：
  startBlockRow = 随机 0 到 blockCount-1
  startBlockCol = 随机 0 到 blockCount-1
  约束：起点区块不在地图正中心
  （如果 blockCount 是奇数，避免 row=floor(blockCount/2) 且 col=floor(blockCount/2)）

终点区块：
  endBlockRow = 随机 0 到 blockCount-1
  endBlockCol = 随机 0 到 blockCount-1
  约束：
    曼哈顿距离 = |endBlockRow - startBlockRow| + |endBlockCol - startBlockCol| ≥ 3
    终点区块不在地图边缘
    （endBlockRow 不在 {0, blockCount-1}，endBlockCol 不在 {0, blockCount-1}）
```

### 6.2 规划区块级路径

在区块层面规划路径网络，确定哪些区块是"活跃区块"。

```
步骤1：生成主路径
从起点区块出发，用带权重的随机游走向终点区块走：
  - 朝向终点的方向权重 ×3
  - 直走（保持当前方向）权重 ×2
  - 偏离终点的方向权重 ×1
主路径经过的区块加入 mainPathBlocks 列表

步骤2：生成替代路径（1-2条）
从主路径上选择两个不相邻的区块作为替代路径的起点和终点
用随机游走生成连接路径
替代路径经过的区块加入 altPathBlocks 列表

步骤3：生成分支
从主路径和替代路径上的区块向外延伸分支：
  对于路径上的每个区块，以概率40%向侧向延伸
  分支延伸1-2个区块
  分支经过的区块加入 branchBlocks 列表

步骤4：标记活跃区块
活跃区块 = mainPathBlocks ∪ altPathBlocks ∪ branchBlocks
未被标记的区块 = 边缘区块

步骤5：标记起点和终点区块
startBlock.isActive = true
endBlock.isActive = true
（它们已经在主路径上，但确保标记）
```

### 6.3 输出

- startBlock 和 endBlock 的位置
- 每个 Block 的 isActive 标记
- mainPathBlocks、altPathBlocks、branchBlocks 列表

---

## 七、阶段三：确定区块合并方案

### 7.1 合并条件

两个相邻区块可以合并，条件：
- 两个区块在同一个方向相邻（上下或左右）
- 两个区块都没有被其他合并占用
- 合并后的联合空间足够容纳一个14×14到16×16的房间
- 合并不发生在起点区块或终点区块（起点和终点房间独立使用单个区块）

### 7.2 合并算法

```
步骤1：确定需要的超大房间数量
超大房间（14×14到16×16）占总房间数的10%
先估算总房间数：
  平均每区块房间数 ≈ 1.5（活跃区块）或 0.5（边缘区块）
  预计总房间数 ≈ 活跃区块数 × 1.5 + 边缘区块数 × 0.5
超大房间数 = max(1, floor(预计总房间数 × 0.1))

步骤2：选择合并的区块对
候选对 = 所有相邻的、活跃的、未被占用的区块对
从候选对中随机选择超大房间数个区块对

对于选中的每对区块：
    blockA.isMerged = true
    blockB.isMerged = true
    blockA.mergedWith = blockB.id
    blockB.mergedWith = blockA.id
    将这两个区块标记为一个"联合区块"

步骤3：确保合并区块不在起点或终点
如果起点或终点区块被选中合并，跳过，重新选择
```

### 7.3 输出

- 合并区块对列表
- 每个 Block 的 isMerged 和 mergedWith 字段

---

## 八、阶段四：放置房间

### 8.1 房间尺寸分档

| 档位 | 尺寸范围 | 占房间总数比例 | 放置位置 |
|------|----------|---------------|----------|
| 小型 | 4×4 到 6×6 | 40% | 任意区块 |
| 中型 | 7×7 到 10×10 | 35% | 活跃区块为主 |
| 大型 | 11×11 到 13×13 | 15% | 活跃区块 |
| 超大型 | 14×14 到 16×16 | 10% | 合并区块 |

### 8.2 放置顺序

**优先放置超大型房间**（因为它们需要合并区块的特殊空间），然后放大型，最后放中小型。

### 8.3 超大型房间放置（14×14到16×16）

```
对于每个合并区块对：
    1. 随机房间尺寸：
       width = 随机 14-16
       height = 随机 14-16
       可以不等（如14×16）
    
    2. 确定放置位置：
       合并区块覆盖的格范围 = 两个区块合并后的矩形
       房间在这个矩形内随机放置
       约束：
         - 房间不超出合并区块范围
         - 房间距离合并区块边界至少1格（留给墙壁和通道）
    
    3. 记录房间：
       创建 Room 对象
       room.blocks = [blockA.id, blockB.id]
       blockA.rooms.append(room.id)
       blockB.rooms.append(room.id)
    
    4. 在地图上标记房间占用格：
       房间内部格子 → "room_floor"
       房间边界格 → "room_wall"
```

### 8.4 大型房间放置（11×11到13×13）

```
大型房间数量 = floor(预计总房间数 × 0.15)

对于每个大型房间：
    1. 随机尺寸：width = 随机 11-13，height = 随机 11-13
    2. 选择一个活跃区块（未被合并占用的、还有空间的）
    3. 在该区块内随机放置，约束：
         - 房间完全在区块内
         - 与已有房间保持至少2格间距
         - 距离区块边界至少1格
    4. 如果10次尝试后仍无法放置，减小尺寸到中型范围重试
```

### 8.5 中、小型房间放置

```
剩余房间数量按比例分配：中型35%，小型40%

放置顺序：先中后小

对于每个房间：
    1. 随机尺寸（根据档位）
    2. 选择一个区块：
         - 活跃区块优先（还有空间的）
         - 边缘区块其次
         - 区块的 isMerged 为 false（没有被合并）
    3. 在该区块内随机放置，约束：
         - 与已有房间至少2格间距
         - 距离区块边界至少1格
         - 房间不超出区块边界
    4. 如果一个区块已放置了多个房间，允许但不强制（小型房间可以密集）
    5. 如果该区块放不下，尝试相邻区块
    6. 如果所有候选区块都放不下，跳过该房间
```

### 8.6 起点房间和终点房间

```
起点房间：
  - 放置在起点区块内
  - 尺寸：5×5 或 6×6
  - type = "start"
  - 强制放置，优先级最高
  - 如果起点区块空间不足，占用区块内最好的位置

终点房间：
  - 放置在终点区块内
  - 尺寸：6×6 或 7×7
  - type = "end"
  - 强制放置，优先级最高
```

### 8.7 房间放置后的处理

```
检查每个区块的 isActive 标记：
  - 活跃区块如果没有任何房间（可能因为空间不足），补充放置至少1个小型房间
  - 边缘区块如果没有房间，可以不放置（该区块将完全是墙壁/通道区域）
```

### 8.8 输出

- 完整的 rooms 列表
- 每个 Block 的 rooms 字段已填充
- 地图上房间区域已标记

---

## 九、阶段五：分配房间类型

### 9.1 房间类型定义

| 类型 | 尺寸倾向 | 出口数 | 内部特征 |
|------|----------|--------|----------|
| start | 5×5到6×6 | 2-3 | 安全，玩家出生点 |
| end | 6×6到7×7 | 2-3（入口） | 中心有遗物台座 |
| hall | 8×8以上 | 3-4 | 开阔大厅，有柱子 |
| chamber | 4×4到10×10 | 1-2 | 标准房间，有宝箱 |
| vault | 5×5到10×10 | 1 | 封闭房间，高价值宝箱 |
| crossing | 6×6到10×10 | 4 | 十字路口，中央地标 |
| deadend | 4×4到6×6 | 1 | 死胡同，宝箱在深处 |
| hidden | 4×4到8×8 | 1（特殊入口） | 隐藏房间，需胶囊进入 |

### 9.2 分配规则

```
对于每个房间：

    if room 是起点房间 → type = "start"
    else if room 是终点房间 → type = "end"
    
    else if 房间在合并区块内（超大型房间）：
        type = "hall"（概率70%）或 "crossing"（概率30%）
    
    else if 房间所在区块在主路径和替代路径的交汇处：
        type = "crossing"（概率60%）或 "hall"（概率40%）
    
    else if 房间所在区块在主路径上（非交汇）：
        type = "hall"（概率50%）或 "chamber"（概率50%）
    
    else if 房间所在区块在分支路径上：
        if 房间是分支末端区块的唯一/最后一个房间：
            type = "deadend"（概率70%）或 "vault"（概率30%）
        else：
            type = "chamber"（概率70%）或 "vault"（概率30%）
    
    else if 房间所在区块在边缘区块（不在任何路径上）：
        type = "vault"（概率50%）或 "hidden"（概率50%）
    
    else：
        type = "chamber"（默认）
```

---

## 十、阶段六：生成房间内部结构

### 10.1 各类型内部结构

**start 房间**：
```
内部完全空旷
玩家出生点：房间中央
墙壁上预留2-3个门口位置（阶段七确定具体位置）
地板使用特殊标记
```

**end 房间**：
```
内部完全空旷
中央：2×2的"遗物台座"（不可通行，装饰性地块）
遗物放在台座中央
墙壁上预留2-3个门口位置
```

**hall 房间**：
```
内部有0-3根柱子（1×1不可通行格）
柱子放置规则：
  - 随机选择内部位置
  - 柱子不能堵住门口之间的直线路径
  - 柱子之间至少间隔1格
  - 柱子不贴墙（至少离墙1格）
```

**chamber 房间**：
```
内部完全空旷或有1根柱子
如果房间尺寸≥7×7，有概率放1根中心柱
```

**vault 房间**：
```
内部有1-2面半墙（1格宽、2-3格长的短墙）
半墙制造视线遮挡
半墙不堵路，但让宝箱不会一进门就看到
宝箱放在最深处
```

**crossing 房间**：
```
内部中央有1个地标（2×2不可通行格，如雕像/喷泉）
四个方向的门口围绕地标分布
地标与门口之间保持通畅
```

**deadend 房间**：
```
内部完全空旷
宝箱放在离门口最远的位置
```

**hidden 房间**：
```
内部完全空旷
所有边界墙壁标记为 "special_wall"
入口方向的那面墙保留1格宽的开口，但用 "special_wall" 标记
需要对应胶囊才能打开
```

### 10.2 生成算法

```
对于每个房间：
    1. 确定房间的内部区域（去除墙壁后的矩形）
    2. 根据房间类型，在内部放置柱子/半墙/地标
    3. 记录 interiorWalls 列表
    4. 在地图上标记对应格子
```

---

## 十一、阶段七：生成房间之间的通道连接

### 11.1 构建房间连接关系

**步骤1：构建候选连接图**

```
以每个房间的中心点为节点
对每个房间，找到距离最近的K个邻居（K=5）
在两个邻居之间建立候选连接边
边的权重 = 两个房间中心点之间的欧几里得距离
```

**步骤2：生成最小生成树（保证全连通）**

```
在候选连接图上运行 Prim 算法
生成最小生成树
最小生成树的边 = 必须存在的通道连接
```

**步骤3：添加额外连接（创造回路）**

```
对于最小生成树之外的候选边：
    以概率 P_extra 添加额外连接
    
    P_extra 取值：
    - 活跃区块内的房间之间：40%
    - 活跃区块和分支区块之间：25%
    - 边缘区块之间：15%
    
    额外连接数量上限 = 房间总数 × 0.25
```

**步骤4：处理特殊房间**

```
vault 类型：只保留1条连接（取最小生成树中权重最小的那条）
hidden 类型：只保留1条连接，且这条连接的入口标记为特殊墙壁
deadend 类型：只保留1条连接
crossing 类型：确保至少有3条连接（如果不足，补充最近的候选边）
```

**步骤5：验证多路径**

```
从起点房间出发，BFS遍历房间连接图
确认终点房间可达
确认从起点到终点至少有2条不相交的路径
如果不满足，在起点和终点路径上的两个不同分支之间手动添加连接
```

### 11.2 生成通道

**步骤1：确定门口位置**

```
对于每对有连接的房间（RoomA, RoomB）：
    1. 确定两个房间的相对方向
       计算 RoomA 中心到 RoomB 中心的向量
    
    2. 在 RoomA 的墙壁上选择门口位置：
       选择最朝向 RoomB 的那面墙
       在该面墙上开1-2格宽的门口
       门口位置：墙面中间区域，避开墙角（至少离墙角1格）
    
    3. 在 RoomB 的墙壁上同样选择门口位置
    
    4. 门口格子类型从 "room_wall" 改为 "door"
```

**步骤2：生成连接通道**

```
对于每对有连接的房间：
    从 RoomA 的门口出发，向 RoomB 的门口方向延伸
    
    使用 A* 寻路算法生成通道：
       起点：RoomA 的门口格（房间外部相邻格）
       终点：RoomB 的门口格（房间外部相邻格）
       地图：当前100×100格
       可通行格：未标记的格、"door"格
       不可通行格：所有墙壁格、房间内部格、已有通道格
       启发式：曼哈顿距离
       移动方向：四方向（上下左右）
    
    A* 找到的路径上的格子标记为 "floor"（通道）
    
    通道宽度：
       主路径上的连接：2格宽
       其他连接：1格宽
    
    通道风格：
       允许直角转弯
       通道不能穿过任何房间内部
       通道经过已有通道时可以合并（共用）
```

**步骤3：门口最终处理**

```
所有 "door" 格最终改为 "floor"
这样门口自然融入通道
```

---

## 十二、阶段八：填充剩余空间

### 12.1 分支通道

```
从已有通道或房间门口出发：
    选择一些未连接到任何东西的通道端点
    以概率50%向空闲空间延伸
    延伸长度：2-8格
    延伸方向：保持当前方向，遇到障碍时随机转弯
    
    延伸路径上的格子标记为 "floor"
    如果延伸遇到另一个通道或房间，连接它们（创造额外回路）
```

### 12.2 小型死胡同

```
在主通道旁随机生成1-3格的小型死胡同
密度：每10格通道长度约1个小型死胡同
死胡同末端可以放置火把残骸或小型宝箱（阶段九处理）
```

### 12.3 封闭区域入口标记

```
对于 vault 房间的入口通道：
    在入口处放置视觉标记（不做代码处理，仅在阶段九放置地标）
    
对于 hidden 房间的入口：
    门口格子标记为 "special_wall"
    旁边放置微弱标记（阶段九处理）
```

---

## 十三、阶段九：放置宝箱和特殊物品

### 13.1 宝箱放置

| 房间类型 | 宝箱数量 | 位置 | 品质 |
|----------|----------|------|------|
| start | 0 | - | - |
| end | 1 | 遗物台座旁 | 豪华 |
| hall | 1-2 | 柱子旁或墙角 | 普通或条件 |
| chamber | 1-2 | 远离门口 | 普通 |
| vault | 2-3 | 最深处 | 提升一级 |
| crossing | 0-1 | 地标旁 | 豪华 |
| deadend | 1-2 | 最深处 | 条件宝箱概率50% |
| hidden | 1-2 | 房间中央/深处 | 最高品质，必含稀有胶囊 |

### 13.2 通道中的补给

```
火把残骸：
  密度：每5-8条通道有1个
  位置：通道旁或小型死胡同末端
  效果：经过自动拾取，恢复30或60秒火把时间

小型宝箱：
  密度：每8-12条通道有1个
  位置：通道交汇处或死胡同末端
  内容：随机消耗型胶囊
```

### 13.3 特殊地形

```
独石碑（地标）：
  密度：每3-5个房间之间的通道交汇处
  作用：纯视觉，帮助玩家建立空间认知

残垣断壁（可通行的假墙）：
  密度：每张地图3-5个
  位置：封闭区域入口附近

高消耗格：
  密度：每张地图5-8个
  位置：捷径处
  效果：经过该格消耗双倍火把
```

### 13.4 特殊标记

```
hidden 房间入口标记：
  在 hidden 房间的特殊墙壁旁放置微弱标记
  助燃剂激活时可见闪烁提示
```

---

## 十四、阶段十：验证

### 14.1 连通性验证

```
从起点房间的出生格出发，BFS遍历所有 "floor" 和 "room_floor" 格
确认：
  - 终点房间的遗物格在可达集合中
  - 所有宝箱所在格在可达集合中
  - 所有房间的至少一个门口在可达集合中
  - hidden 房间的特殊墙壁确实封闭（不在可达集合中）
```

### 14.2 路径长度验证

```
计算从起点到终点的最短路径（沿 floor 格走）
最短路径应在 80-250 格之间
如果太短：在路径上增加额外障碍或弯折
如果太长：检查是否有必要，如果合理则保留
```

### 14.3 房间密度验证

```
房间总数应在 20-50 个范围内
房间之间的平均通道长度应在 5-20 格之间
如果通道太长（>20格且超过3条），在中间补充 chamber 或 deadend
```

### 14.4 宝箱分布验证

```
检查连续5个房间以上没有宝箱 → 补充
检查 vault 和 hidden 房间是否有宝箱 → 补充
检查起点房间无宝箱 → 移除
检查终点房间有宝箱 → 确保
```

### 14.5 死路验证

```
统计只有1个出入口的房间数量
这类房间应在 25%-45% 之间
如果太少：将部分 chamber 改为 deadend
如果太多：给部分 deadend 增加额外连接
```

---

## 十五、完整生成流程伪代码

```
function GenerateFullMazeV5():
    
    // 阶段一
    x = RandomInt(12, 20)
    blockCount = floor(100 / x)
    blocks = CreateBlockGrid(blockCount, x)
    MarkBorderWalls(blocks, blockCount, x)
    
    // 阶段二
    startBlock = SelectStartBlock(blocks)
    endBlock = SelectEndBlock(blocks, startBlock, minDist=3)
    mainPathBlocks = GenerateMainPath(blocks, startBlock, endBlock)
    altPathBlocks = GenerateAltPaths(blocks, mainPathBlocks)
    branchBlocks = GenerateBranches(blocks, mainPathBlocks, altPathBlocks)
    MarkActiveBlocks(blocks, mainPathBlocks, altPathBlocks, branchBlocks)
    
    // 阶段三
    estimatedRoomCount = EstimateRoomCount(blocks)
    megaRoomCount = Max(1, floor(estimatedRoomCount * 0.1))
    mergedPairs = SelectMergePairs(blocks, megaRoomCount, startBlock, endBlock)
    ApplyMerges(blocks, mergedPairs)
    
    // 阶段四
    rooms = []
    PlaceMegaRooms(rooms, blocks, mergedPairs)    // 14-16格
    PlaceLargeRooms(rooms, blocks, floor(estimatedRoomCount * 0.15))  // 11-13格
    PlaceMediumRooms(rooms, blocks, floor(estimatedRoomCount * 0.35)) // 7-10格
    PlaceSmallRooms(rooms, blocks, floor(estimatedRoomCount * 0.40))  // 4-6格
    PlaceStartRoom(rooms, startBlock)
    PlaceEndRoom(rooms, endBlock)
    FillEmptyActiveBlocks(blocks, rooms)
    
    // 阶段五
    AssignRoomTypes(rooms, blocks, mainPathBlocks, altPathBlocks, branchBlocks)
    
    // 阶段六
    for each room in rooms:
        GenerateRoomInterior(room)
    
    // 阶段七
    candidateEdges = BuildKNNEdges(rooms, K=5)
    mst = PrimMST(candidateEdges)
    extraEdges = AddExtraEdges(candidateEdges, mst, blocks)
    allEdges = mst + extraEdges
    ProcessSpecialRooms(allEdges, rooms)  // vault/hidden/deadend/crossing
    ValidateMultiPath(allEdges, rooms, startBlock, endBlock)
    GenerateCorridors(allEdges, rooms)
    
    // 阶段八
    GenerateBranchCorridors()
    GenerateSmallDeadEnds()
    MarkSpecialEntrances(rooms)
    
    // 阶段九
    PlaceTreasureChests(rooms)
    PlaceCorridorPickups()
    PlaceSpecialTerrain()
    PlaceHiddenMarkers(rooms)
    
    // 阶段十
    ValidateConnectivity(startRoom, endRoom, rooms)
    ValidatePathLength(startRoom, endRoom, 80, 250)
    ValidateRoomDensity(rooms, 20, 50)
    ValidateChestDistribution(rooms)
    ValidateDeadEndRatio(rooms, 0.25, 0.45)
    
    return map
```

---

## 十六、参数速查表

| 参数 | 建议值 | 说明 |
|------|--------|------|
| 大地图尺寸 | 100×100格 | 固定 |
| x | 随机12-20 | 决定区块数量 |
| 区块数量 | floor(100/x)² | 25-64个 |
| 房间总数 | 20-50个 | 动态 |
| 小房间(4-6)比例 | 40% | - |
| 中房间(7-10)比例 | 35% | - |
| 大房间(11-13)比例 | 15% | - |
| 超大房间(14-16)比例 | 10% | 需区块合并 |
| 房间最小间距 | 2格 | - |
| 合并区块上限 | 2个 | 相邻 |
| 起点终点最小距离 | 3个区块 | 曼哈顿距离 |
| 分支延伸长度 | 1-2个区块 | - |
| 分支生成概率 | 40% | 每个路径区块 |
| K近邻 | 5 | 候选连接 |
| 额外连接概率(活跃) | 40% | - |
| 额外连接概率(分支) | 25% | - |
| 额外连接概率(边缘) | 15% | - |
| 额外连接上限 | 房间总数×0.25 | - |
| 主路径通道宽度 | 2格 | - |
| 其他通道宽度 | 1格 | - |
| 分支通道长度 | 2-8格 | - |
| 最短路径范围 | 80-250格 | 起点到终点 |
| 死路房间比例 | 25%-45% | - |

---

以上是 v5 完整方案，可直接用于 vibe coding 实现。核心改动是动态区块+灵活房间尺寸+区块合并支持超大房间，解决了 v4 的僵硬问题。建议按阶段逐步实现并验证，特别是阶段四的房间放置逻辑，需要多测试不同 x 值下的表现。